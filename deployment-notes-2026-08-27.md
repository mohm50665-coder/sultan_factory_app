تم فتح لوحة cPanel العربية على المنفذ 2083 بحساب المستخدم الذي أدخله صاحب المشروع. مسار إعداد Node.js هو lveversion/nodejs-selector.html.tt، لكنه بقي في حالة Loader عند آخر فحص. لوحة cPanel تعرض أيضاً إدارة الملفات. النسخة المحلية الجاهزة للنشر موجودة في dist بعد نجاح pnpm test (81/81) وpnpm build، وتحتاج رفع dist/index.js وdist/web وإعادة تشغيل تطبيق Node.js. لا توجد نية لتعديل قاعدة البيانات أثناء الرفع؛ أعمدة deliveredAt وreceivedAt سبق ترحيلها بأمان.
في مدير الملفات يجب استخدام المسار النسبي sultan-node1 من الدليل /home/almaghrbi؛ إدخال المسار الكامل أثناء وجود الدليل الرئيسي يكرر home/almaghrbi. بعد فتح dir الصحيح ظهرت ملفات المشروع الحالية وdist، والتطبيق المقصود هو almaghrbi.com/ ومساره /home/almaghrbi/sultan-node1 وحالته started بإصدار Node v20.20.2.
تم استخراج sultan-node-update-12acd459.zip داخل /home/almaghrbi/sultan-node1، وظهرت نتائج الاستخراج متضمنة dist/index.js وdist/web/barcode-labels.html وdist/web/product-tracking.html وdist/web/users-management.html. بعد ذلك تمت إعادة تشغيل تطبيق almaghrbi.com من إعداد Node.js، وعادت حالته إلى started (v20.20.2) مع ظهور خيار Restart the application.
بعد إعادة التشغيل، رابط almaghrbi.com يفتح واجهة المصنع، لكن فتح /product-tracking مباشرة أعاد المتصفح إلى /login لأن جلسة الدخول غير موجودة في جلسة التحقق الحالية. هذا سلوك حماية متوقع وليس دليلاً على فشل التحديث؛ يلزم تسجيل الدخول ثم فحص الأيقونات والصفحات من داخل الجلسة.
اكتمل رفع الحزمة واستخراجها وإعادة تشغيل تطبيق almaghrbi.com، وبقيت حالة التطبيق started (v20.20.2) مع المؤشر الأخضر. تم فتح الموقع الحي بنجاح وظهرت لوحة المصنع وبها أيقونة تتبع المنتجات المستقلة. فتح المسارات الداخلية مباشرة يعيد إلى /login عند غياب جلسة المستخدم. صفحة localhost:8081 الظاهرة على جهاز المستخدم ليست رابط الاستضافة ولا تعني تعطل الموقع الحي؛ يجب استخدام https://almaghrbi.com.
تمت إعادة تسجيل الدخول إلى cPanel بعد انتهاء الجلسة، ثم الضغط على زر Restart لتطبيق almaghrbi.com من صفحة Node.js. ظل التطبيق في حالة started (v20.20.2) بالمؤشر الأخضر.
[2026-08-29] تم فحص ملف التشغيل الخارجي /home/almaghrbi/sultan-node1/app.cjs عبر cPanel. محتواه: async function main() { await import("./dist/index.js"); } main().catch((err) => { console.error("Startup failed:", err); process.exit(1); });. إعداد Node.js للنطاق almaghrbi.com يستخدم الجذر /home/almaghrbi/sultan-node1 وStartup File=app.cjs، والحالة started، لكن الرابط يعرض 503. الخطوة التالية الآمنة هي فحص سجل Passenger أو خطأ استيراد dist/index.js قبل أي تعديل أو نشر جديد.
[2026-08-29] تم تنفيذ إيقاف كامل ثم تشغيل لتطبيق almaghrbi.com من Node.js Selector. عادت الحالة إلى started (v20.20.2)، لكن الفحص الخارجي للرابط الرئيسي ما زال يعرض 503 Service Unavailable. تم فحص app.cjs ووجد أنه يستدعي ./dist/index.js بشكل صحيح، كما أن app.log يحتوي طلبات Auth بدون session cookie ولا يظهر انهياراً صريحاً. يلزم Passenger/hosting error log أو تدخل المستضيف قبل أي حذف أو نشر إضافي.
[2026-08-29] قراءة app.log بعد إعادة التشغيل أظهرت بوضوح: OAUTH_SERVER_URL غير مهيأ (تحذير OAuth غير قاتل)، ثم `Port 3099 is busy, using port 3100 instead` و`[api] server listening on port 3100`. هذا يثبت أن سبب 503 هو انتقال الخادم من منفذ Passenger المخصص إلى 3100. تم تعديل server/_core/index.ts محلياً لإزالة findAvailablePort والاستماع مباشرة على process.env.PORT مع 0.0.0.0. نجح البناء والاختبارات، وتحتاج الاستضافة رفع dist الجديد ثم إعادة التشغيل.
