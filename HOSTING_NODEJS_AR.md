# تعليمات تشغيل متجر مصنع السلطان على استضافة Node.js

## 1. المتطلبات

- Node.js إصدار 18 أو أحدث، ويفضّل Node.js 20 LTS.
- مدير الحزم pnpm إصدار 9 أو أحدث، أو npm إذا كان pnpm غير متاح.
- قاعدة بيانات MySQL/MariaDB متاحة من الخادم؛ ملف المشروع الحالي يستخدم Drizzle مع `mysql2`.
- نطاق الموقع: `almaghrbi.com` و`www.almaghrbi.com`.

## 2. رفع الملفات

1. حمّل ملف النسخة الاحتياطية وفك الضغط عنه.
2. ارفع محتويات مجلد المشروع إلى مجلد مستقل للتطبيق، وليس إلى مجلد `public_html` وحده إذا كانت لوحة الاستضافة توفر مجلدًا خاصًا لتطبيق Node.js.
3. لا ترفع أي ملف `.env` يحتوي على كلمات مرور أو مفاتيح إلى مستودع عام أو رابط مكشوف.

## 3. تثبيت الحزم

من داخل مجلد المشروع:

```bash
corepack enable
corepack prepare pnpm@9.12.0 --activate
pnpm install --frozen-lockfile
```

إذا لم يتوفر Corepack يمكن استخدام:

```bash
npm install -g pnpm@9.12.0
pnpm install --frozen-lockfile
```

## 4. إعداد متغيرات البيئة

أنشئ ملف `.env` في مجلد المشروع أو أضف المتغيرات من لوحة Node.js في الاستضافة. يجب تزويد القيم الحقيقية من مالك قاعدة البيانات والمستضيف:

```env
NODE_ENV=production
DATABASE_URL=mysql://DB_USER:DB_PASSWORD@DB_HOST:3306/DB_NAME
JWT_SECRET=ضع_قيمة_سرية_طويلة_وعشوائية
VITE_APP_ID=قيمة_معرّف_التطبيق_إن_كانت_مطلوبة
OWNER_OPEN_ID=معرّف_المالك_إن_كان_مطلوباً
OAUTH_SERVER_URL=رابط_خدمة_تسجيل_الدخول_إن_كانت_مفعلة
BUILT_IN_FORGE_API_URL=اتركه_فارغاً_إذا_لم_تستخدم_ميزات_الخدمات_المدمجة
BUILT_IN_FORGE_API_KEY=اتركه_فارغاً_إذا_لم_تستخدم_ميزات_الخدمات_المدمجة
```

يجب عدم إرسال كلمات المرور أو مفاتيح البيئة داخل مجموعة الواتساب. تُضاف هذه القيم في لوحة الاستضافة أو عبر ملف `.env` محمي بصلاحيات الخادم.

## 5. إنشاء نسخة الإنتاج

نفّذ الأوامر التالية:

```bash
pnpm build
```

ينشئ الأمر نسخة الويب داخل `dist/web` ويبني خادم Node.js داخل `dist/index.js`.

## 6. تشغيل التطبيق

شغّل التطبيق بالأمر:

```bash
pnpm start
```

أو اضبط الحقول التالية في لوحة **Setup Node.js App**:

| الحقل | القيمة |
|---|---|
| Application root | مسار مجلد المشروع |
| Application URL | `almaghrbi.com` |
| Application startup file | `dist/index.js` |
| Application mode | `Production` |
| Node.js version | 20 LTS أو 18 LTS |
| Startup command | `pnpm start` أو أمر التشغيل الذي توفره اللوحة |
| Port | المنفذ الذي تحدده لوحة الاستضافة عبر `PORT` |

إذا كانت لوحة الاستضافة تتطلب ملف بدء باسم `app.js`، يجب إنشاءه أو تحديد `dist/index.js` مباشرة حسب تعليمات اللوحة. لا يتم تشغيل ملف TypeScript مباشرة في الإنتاج؛ يتم تشغيل ملف `dist/index.js` الناتج من البناء.

## 7. قاعدة البيانات

أنشئ قاعدة بيانات MySQL أو MariaDB فارغة، ثم ضع بيانات الاتصال في `DATABASE_URL`. بعد ذلك تُطبَّق جداول المشروع بالطريقة التي يعتمدها المستضيف، مع أخذ نسخة احتياطية من قاعدة البيانات قبل أي ترحيل:

```bash
pnpm db:push
```

إذا كانت الاستضافة لا تسمح بتنفيذ أوامر الترحيل، يزوّد المستضيف فريق التطوير برسالة الخطأ وبيانات إصدار MySQL/MariaDB، ولا يغيّر الجداول يدوياً قبل أخذ نسخة احتياطية.

## 8. ربط النطاق وHTTPS

بعد نجاح تشغيل التطبيق، يربط المستضيف النطاق بالتطبيق من لوحة Node.js ويصدر شهادة SSL للنطاقين:

- `almaghrbi.com`
- `www.almaghrbi.com`

يجب اختبار الموقع عبر `https://almaghrbi.com`، ثم اختبار تسجيل الدخول وحفظ البيانات والإنتاج والمبيعات والمستودعات والتقارير والمرفقات.

## 9. معلومات مهمة

هذا المشروع ليس مشروع PHP تقليدياً. الواجهة مبنية بـ React/Expo للويب، والخادم مبني بـ Node.js/Express وtRPC. لا ينبغي تحويل الملفات إلى PHP؛ المطلوب هو تشغيل Node.js وربط قاعدة البيانات الصحيحة.

في حال ظهور خطأ، يرسل المستضيف نص الخطأ الكامل من سجلات Node.js مع إصدار Node.js ونتيجة `pnpm build`، دون إرسال كلمات المرور أو قيم الأسرار.
