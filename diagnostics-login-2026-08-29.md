# تشخيص دوران تسجيل الدخول — 2026-08-29

## النتائج المؤكدة

- الرابط البديل `https://sultanapp-paotprzy.manus.space/` يفتح واجهة مصنع السلطان.
- نقطة الصحة `GET /api/health` استجابت بنجاح بـ `{"ok":true,...}`.
- الاستعلام `GET /api/trpc/auth.me?input={"json":null}` استجاب بنجاح بـ `{"result":{"data":{"json":null}}}` عند عدم وجود جلسة.
- اختبار `POST /api/trpc/auth.login` ببيانات تشخيصية غير حقيقية استجاب خلال نحو 3.5 ثوانٍ برسالة اسم المستخدم أو كلمة المرور غير صحيحة؛ هذا يثبت أن مسار API وقاعدة البيانات يستجيبان ولا يوجد تعليق عام في الطلب.

## كود الواجهة

- `app/login.tsx` يضع `isLoading` قبل `login` ثم يستدعي `router.replace("/(tabs)")` عند نجاح `login`، ويستخدم `Alert.alert` عند الخطأ.
- `lib/auth-context.tsx` يرسل طلب tRPC بصيغة `{json: {username, password}}` ويقرأ الاستجابة من `data.result.data.json`، وهو متوافق مع استجابة الخادم.
- لا يوجد مهلة timeout في `apiCall`; إذا تعطل اتصال حقيقي فقد يبقى زر الدخول في حالة الدوران.
- لا يجوز استخدام بيانات اعتماد حقيقية في فحص آلي جديد دون تأكيد المستخدم؛ الاختبار المنفذ كان ببيانات غير حقيقية فقط.

## الاستنتاج المرحلي

المشكلة ليست توقف API العام. يلزم التحقق من نتيجة الحساب الفعلي (صحيح/غير مفعّل/غير صحيح) ومن ظهور رسالة الخطأ على الويب، ثم إضافة مهلة ورسالة مرئية بدلاً من الاعتماد على `Alert.alert` فقط إذا ثبت أن الخطأ لا يظهر للمستخدم.
